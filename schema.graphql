schema {
  query: Query
  # mutation: Mutation
}

scalar Time

type User {
  id: ID!
  email: String
  createdAt: Time
}

type Query {
  users: User!
}

# type Mutation {
#   createNote(userID: ID!, note: NoteInput!): Note!
# }
