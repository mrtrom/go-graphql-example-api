schema {
  query: Query
  # mutation: Mutation
}

scalar Time

type User {
  id: ID!
  name: String
  lastName: String
}

type Query {
  users: User!
}

# type Mutation {
#   createNote(userID: ID!, note: NoteInput!): Note!
# }
