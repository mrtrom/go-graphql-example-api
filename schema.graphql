schema {
  query: Query
  # mutation: Mutation
}

scalar Time
scalar Date
scalar DateTime

type User {
  id: ID!
  createdAt: Time
  updatedAt: Time
  name: String
  username: String!
  email: String!
}

type Query {
  users: User!
}

# type Mutation {
#   createNote(userID: ID!, note: NoteInput!): Note!
# }
